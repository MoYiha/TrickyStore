# Additional GPLv3 section 7(b) attribution term for tryigit-owned material: see ../../NOTICE.
[package]
name = "cleverestricky-attestation-core"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"
publish = false

[dependencies]
der = { version = "=0.8.1", features = ["alloc"] }

[lints.rust]
unsafe_code = "forbid"
