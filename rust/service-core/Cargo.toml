# Additional GPLv3 section 7(b) attribution term for tryigit-owned material: see ../../NOTICE.
[package]
name = "cleverestricky-service-core"
version = "0.1.0"
edition = "2021"
description = "Bounded portable service primitives for CleveresTricky"
license = "GPL-3.0-only"

[dependencies]
libc = "0.2"
zeroize = { version = "1.8", features = ["derive"] }
