name: "CodeQL"

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  schedule:
    - cron: '0 0 * * 0'

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'java-kotlin' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}

    - name: Set up Kotlin for CodeQL compatibility
      uses: gradle/actions/setup-gradle@v6
      with:
        cache-disabled: true

    - name: Pin Kotlin version for CodeQL
      run: sed -i 's/^kotlin = ".*"/kotlin = "2.3.10"/' gradle/libs.versions.toml

    # Use explicit Gradle invocation (instead of CodeQL autobuild) to force in-process Kotlin compiler
    # and avoid extractor incompatibility with the default Kotlin build tools API path.
    - name: Autobuild
      run: ./gradlew testDebugUnitTest -Pkotlin.compiler.execution.strategy=in-process

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:${{matrix.language}}"
